class CardUtils {
    static shuffle(deck) {
        // 洗牌算法实现
    }

    static getCardType(cards) {
        // 牌型验证逻辑（与Flutter端CardType一致）
    }

    static isBigger(newCards, oldCards) {
        // 牌型大小比较逻辑
    }

    static sortCards(cards) {
        // 排序逻辑（与Flutter端一致）
    }
}

module.exports = CardUtils;